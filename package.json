{
  "name": "mendr",
  "version": "0.0.0",
  "description": "Autonomous PR review agent CLI scaffold.",
  "type": "module",
  "bin": {
    "mendr": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/cli.ts src/daemon.ts --format esm --dts --clean --external react",
    "coverage": "vitest run --coverage",
    "test": "vitest run",
    "test:e2e": "MENDR_E2E=1 vitest run test/e2e",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "execa": "^9.6.0",
    "ink": "^5.2.1",
    "ink-spinner": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^18.3.31",
    "@vitest/coverage-v8": "^2.1.9",
    "react": "^18.3.1",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
